  {
  "layer": "top",
  "position": "top",

  "exclusive": true,
  "passthrough": false,

  "margin-top": 8,
  "margin-left": 12,
  "margin-right": 12,

  "custom/nvidia": {
  "exec": "nvidia-smi --query-gpu=utilization.gpu,temperature.gpu,power.draw --format=csv,noheader,nounits | awk -F', ' '{printf \"%s%% %s°C %.0fW\", $1, $2, $3}'",
  "interval": 5,
  "format": " {}",
  "tooltip": true,
  "on-click": "kitty -e nvidia-smi -l 1"
},


  "modules-left": [
    "hyprland/workspaces",
    "hyprland/window"
  ],

  "modules-center": [
    "mpris",
    "clock"
  ],

  "modules-right": [
    "cpu",
    "memory",
    "temperature",
    "custom-gpu",
    "pulseaudio",
    "network",
    "battery",
    "tray"
  ],

  "hyprland/workspaces": {
    "disable-scroll": true,
    "all-outputs": true
  },

  "hyprland/window": {
    "max-length": 40,
    "separate-outputs": true
  },

  "mpris": {
    "format": " {artist} - {title}",
    "format-paused": "",
    "player-icons": {
      "default": ""
    },
    "max-length": 40,
    "on-click": "playerctl play-pause",
    "on-scroll-up": "playerctl next",
    "on-scroll-down": "playerctl previous"
  },

  "clock": {
    "format": "{:%a %d %b  %I:%M %p}",
    "tooltip": true
  },

  "cpu": {
    "interval": 5,
    "format": " {usage}%",
    "on-click": "kitty -e btop"
  },

  "memory": {
    "interval": 5,
    "format": " {percentage}%",
    "on-click": "kitty -e btop"
  },

  "temperature": {
    "interval": 5,
    "format": " {temperatureC}°C",
    "critical-threshold": 80
  },

  "custom-gpu": {
    "exec": "cat /sys/class/drm/card0/device/gpu_busy_percent 2>/dev/null || echo 0",
    "interval": 5,
    "format": " {}%",
    "on-click": "kitty -e btop"
  },

  "pulseaudio": {
    "format": "{icon} {volume}%",
    "format-muted": " muted",
    "format-icons": {
      "default": ""
    },
    "on-click": "pavucontrol"
  },

  "network": {
    "format-wifi": " {signalStrength}%",
    "format-ethernet": "󰈀 wired",
    "format-disconnected": "󰤭",
    "tooltip": true,
    "on-click": "nm-connection-editor"
  },

  "battery": {
    "interval": 30,
    "format": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-icons": ["","","","",""]
  },

  "tray": {
    "icon-size": 16,
    "spacing": 8
  }
}
